# HouseFolio 开发日志｜2026-05-02｜Phase 2A–2C LBS 基础层

## 1. 本日志用途

本日志记录 HouseFolio 在 2026-05-02 完成的 Phase 2A–2C 进展。

如果后续新开对话继续 HouseFolio，请先阅读本日志，避免倒退到 Phase 1M / Phase 1N / Phase 2A，也避免误以为已经完成通勤计算、POI 搜索、地图视图或页面接入。

当前最高优先级仍然是：

```text
基础闭环稳定
架构边界清晰
合规边界明确
后续迁移友好
2. 当前项目定位

HouseFolio 仍然是面向中国大陆租客的私人找房决策管理工具。

继续坚持：

不抓取第三方房源页面；
不搬运贝壳、58、小红书、豆瓣等平台内容；
不公开用户房源库；
不做房东端、预约看房、联系房东、中介撮合、佣金、保证金；
不宣传“真房源”“避坑保真”“替你选房负责”。

三层引擎边界继续保持：

L1 LBS：通勤、生活圈、地图、空间关系
L2 算法：参考评分、排序、筛选、对比、异常提示
L3 AI：总结、建议、解释、checklist、风险信号人话化
3. Phase 2A：工作/学习地点（通勤锚点）本地设置

Phase 2A 已完成。

完成内容：

新增工作/学习地点数据模型；
新增 localStorage 存储；
Settings 页面新增工作/学习地点设置面板；
支持新增、查看、删除通勤锚点；
本地数据快照可显示 housefolio:work-locations；
导出 JSON 包含 work-locations；
清除本机数据覆盖该数据类型；
产品定义从“工作地点”修正为“工作/学习地点（通勤锚点）”。

关键文件：

src/types/work-location.ts
src/lib/local-store/work-locations.ts
src/components/work-location-settings-panel.tsx
src/lib/privacy/local-data.ts
src/content/zh-cn.ts

已手动验证：

/settings 页面显示“工作/学习地点（通勤锚点）”；
可添加测试锚点：
我的公司 / 望京 SOHO 附近；
伴侣公司 / 国贸附近；
学校 / 五道口地铁站附近；
列表显示正常；
删除正常；
本地数据快照正常；
导出 JSON 正常。
4. Phase 2B：LBS provider 封装

Phase 2B 已完成。

完成内容：

定义 LBS provider interface；
新增 mock provider；
新增统一 service 出口；
新增 provider registry；
新增 contract check；
当前页面和 UI 没有直接调用高德。

关键文件：

src/lib/lbs/provider.ts
src/lib/lbs/mock-provider.ts
src/lib/lbs/service.ts
src/lib/lbs/registry.ts
src/lib/lbs/index.ts
src/lib/lbs/contract-check.ts

统一出口：

geocodeAddress(input)
calculateCommute(input)
searchNearbyPoi(input)

当前 Phase 2B 只完成封装，不代表真实 LBS 已经全面接入。

5. Phase 2C：高德地理编码前置封装与最小接入

Phase 2C 已完成到 geocode smoke test。

完成内容：

新增 LBS runtime config；
新增 amap provider skeleton；
新增高德请求 / 响应 contract types；
新增 LBS provider boundary 文档；
新增 Amap geocode implementation plan；
实现 amap-provider.ts 内的 geocodeAddress；
本地 smoke script 可用真实 AMAP_API_KEY 测试地理编码；
移除 next/font/google，避免 build 依赖 Google Fonts 网络请求。

关键文件：

src/lib/config/lbs.ts
src/lib/lbs/amap-provider.ts
src/lib/lbs/amap-contract.ts
src/lib/lbs/index.ts
scripts/smoke-amap-geocode.mjs
docs/architecture/lbs-provider-boundary.md
docs/architecture/amap-geocode-implementation-plan.md
src/app/layout.tsx

当前 .env.local 本地配置：

LBS_PROVIDER=amap
AMAP_API_KEY=<local only>

注意：

.env.local 不进入 Git；
.gitignore 中 .env* 已覆盖 .env.local；
不要把真实 key 贴到对话里；
不要把真实 key 提交到 Git。
6. 当前已验证内容

已验证：

npm.cmd run build 通过
git status clean
高德 Web 服务 Key 本地 smoke test 通过
Google Fonts 远程依赖已移除

已确认没有：

NEXT_PUBLIC_AMAP_API_KEY
真实 key 明文提交
页面直接调用高德
缓存高德原始 JSON
地图 UI
通勤计算
POI 搜索
7. 当前尚未完成的内容

虽然 geocode 已经接入，但以下内容尚未完成：

真实通勤计算
真实 POI 搜索
生活圈评分真实计算
地图视图
Settings 页面调用 geocode
Add Listing 页面调用 geocode
Portfolio / Detail 页面展示真实 LBS 数据
LBS 结果写入 localStorage
LBS 结果导出 / 清除
AI 使用 LBS 摘要

不要误判为 L1 LBS 已完整完成。

当前真实完成的是：

高德地理编码 provider 的最小实现与本地 smoke test。
8. 下一步建议

下一步建议进入：

Phase 2D-0：通勤计算 implementation plan

先写文档，不直接接 API。

原因：

通勤计算会涉及路线距离、预计耗时、交通方式；
还要决定只缓存哪些摘要字段；
不能缓存完整路线轨迹或高德原始响应；
需要明确失败降级；
需要继续支持 1–3 个工作/学习地点（通勤锚点）。

Phase 2D-0 应输出：

docs/architecture/amap-commute-implementation-plan.md

暂不做：

真实 calculateCommute
POI 搜索
地图 UI
页面接入
9. 当前 Git 纪律

每个小阶段结束后继续执行：

npm.cmd run build
git status
git add <明确文件路径>
git commit -m "short English commit message"
git status

涉及 .env.local、API key、隐私数据时，不使用 git add .，优先使用明确文件路径。

10. 当前结论

HouseFolio 当前已经完成 L1 LBS 的基础工程骨架：

通勤锚点本地设置
→ LBS provider 封装
→ 高德 geocode 最小接入
→ 本地 smoke test

但它还不是完整 L1 LBS 引擎。

下一步应继续保持小步推进，先规划通勤计算，再决定是否实现 calculateCommute。